# Local Worker Results

The local worker writes machine-readable task results here during V0.1 E2E validation.

Example:

```text
results/task-0001.json
```

Result files are produced by the local machine after verification. They record task status, attempts, changed files, stdout/stderr, and any error.
